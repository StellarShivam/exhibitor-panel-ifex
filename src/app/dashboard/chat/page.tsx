import { ChatView } from 'src/sections/chat/view/index';
// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Chat',
};

export default function Page() {
  return <ChatView />;
}
