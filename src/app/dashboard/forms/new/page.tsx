import { TicketCreateView } from 'src/sections/help-and-support/view';
// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new ticket',
};

export default function TicketCreatePage() {
  return <TicketCreateView />;
}
