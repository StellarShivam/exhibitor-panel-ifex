import { NetworkingListView } from 'src/sections/networking/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Networking',
};

export default function NetworkingPage() {
  return <NetworkingListView />;
}
