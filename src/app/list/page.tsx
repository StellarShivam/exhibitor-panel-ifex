import EventCardsGrid from 'src/sections/list/views';
// ----------------------------------------------------------------------

export const metadata = {
  title: 'Lists: Events',
};

export default function Page() {
  return <EventCardsGrid />;
}
